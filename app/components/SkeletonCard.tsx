export default function SkeletonCard() {
  return (
    <div
      style={{
        height: 300,
        background: "#e5e7eb",
        borderRadius: 16,
        animation: "pulse 1.5s infinite",
      }}
    />
  );
}